% ====================================================================
% test_snapshot_sandbox.trx -- snap-shot / thaw are filesystem writers
% and whole-VM loaders; both are disabled under --sandbox.  Run by
% runtests.sh step 4 and the coverage pipeline (every
% tests/test_*_sandbox.trx executes with --sandbox).
% ====================================================================

% rt-ok counts failures in a restore-immune slot -- string byte writes
% are unjournaled, so the count survives assertions made inside save
% scopes, and the 1-byte string lives in LOCAL VM, invisible to the
% global-heap/gc-counter probes some files assert on.  The file THROWS
% at EOF: test_all counts a file as failed only when its run throws,
% so print-only failures would slip the suite verdict.
/rt-fails 1 string def
rt-fails 0 16#0#b put
/rt-ok { exch { = } { (FAILED: ) print = rt-fails 0  rt-fails 0 get /integer-type cast 1 add dup 255 gt { pop 255 } if /byte-type cast  put } if-else } def

{ (/tmp/sandbox-test.img) snap-shot } try
/unsupported eq (sandbox: snap-shot disabled) rt-ok clear

{ (/tmp/sandbox-test.img) thaw } try
/unsupported eq (sandbox: thaw disabled) rt-ok clear

(snapshot-sandbox: all tests passed) =

rt-fails 0 get /integer-type cast 0 gt { /rt-assertions-failed throw } if
(__test_ok__) =
